from setuptools import setup, find_packages

setup(
    name='lib_managed_string',
    version='0.1',
    packages=find_packages(),
    description='A simple django library to manage strings',
    long_description=open('README.md').read(),
    author='Ankit',
)
